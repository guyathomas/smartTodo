{
  "name": "smartodo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "babel --watch client/src --out-dir client/compiled"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-react": "^6.16.0"
  },
  "babel": {
    "presets": [
      "react"
    ]
  }
}
